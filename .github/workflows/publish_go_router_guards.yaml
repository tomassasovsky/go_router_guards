name: publish/go_router_guards

on:
  push:
    tags:
      - "go_router_guards-v[0-9]+.[0-9]+.[0-9]+*"

jobs:
  publish:
    environment: pub.dev
    runs-on: ubuntu-latest
    permissions:
      id-token: write # Required for authentication using OIDC

    steps:
      - name: 📚 Git Checkout
        uses: actions/checkout@v4

      - name: 📦 Publish
        uses: ./.github/actions/pub_publish
        with:
          working_directory: packages/go_router_guards
